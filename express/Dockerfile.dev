FROM node:22-slim
WORKDIR /app

COPY . .

RUN npm install

ENV PORT=3000

ENV DB_HOST=''
ENV DB_NAME=''
ENV DB_PORT=''
ENV DB_USER=''
ENV DB_PASS=''
ENV CHOKIDAR_USEPOLLING=true

ENV CORS_ALLOWED_ORIGIN='*'

EXPOSE $PORT

CMD ["npm", "run", "dev"]